version: "3.9"

services:
  corta-pm:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "6699:8080"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    environment:
      - NODE_ENV=development
